---
title: 'Potencias y Raíces'
description: 'Domina las operaciones con potencias de base racional y exponente entero, y su operación inversa, las raíces.'
---

## Potencias

Una potencia es una forma abreviada de escribir una multiplicación de factores iguales. Se expresa como `aⁿ`, donde `a` es la **base** y `n` es el **exponente**.

`aⁿ = a * a * a * ...` (n veces)

### Propiedades Clave de las Potencias

- **Multiplicación de potencias de igual base:** `aᵐ * aⁿ = aᵐ⁺ⁿ`
- **División de potencias de igual base:** `aᵐ / aⁿ = aᵐ⁻ⁿ`
- **Potencia de una potencia:** `(aᵐ)ⁿ = aᵐ*ⁿ`
- **Potencia de un producto:** `(a * b)ⁿ = aⁿ * bⁿ`
- **Exponente cero:** `a⁰ = 1` (si `a ≠ 0`)
- **Exponente negativo:** `a⁻ⁿ = 1 / aⁿ`

## Raíces

La raíz es la operación inversa a la potenciación. La raíz n-ésima de un número `a` es un número `b` tal que `bⁿ = a`. Se escribe como `ⁿ√a = b`.

- **Raíz cuadrada:** Es el caso más común, donde `n=2`. Por ejemplo, `√9 = 3` porque `3² = 9`.

### Relación entre Potencias y Raíces

Las raíces se pueden expresar como potencias con exponente fraccionario:

`ⁿ√aᵐ = a^(m/n)`

Por ejemplo, `√a = a^(1/2)` y `³√a² = a^(2/3)`. Esta propiedad es muy útil para simplificar operaciones.

## Ejemplo Práctico: Simplificar una Expresión

**Pregunta:** Simplifica la expresión `(x²)³ * x⁴`.

1.  **Aplicamos la propiedad de "Potencia de una potencia":**
    `(x²)³ = x²*³ = x⁶`

2.  **La expresión se convierte en:**
    `x⁶ * x⁴`

3.  **Aplicamos la propiedad de "Multiplicación de potencias de igual base":**
    `x⁶ * x⁴ = x⁶⁺⁴ = x¹⁰`

**Respuesta:** La expresión simplificada es `x¹⁰`.

## ¿Y en la calculadora científica?

- **Potencias:** Para calcular `aⁿ`, usa el botón `x^y`, `y^x` o `^`. Ejemplo: para `5³`, teclea `5`, luego `x^y`, luego `3` y presiona `=` para obtener `125`. Para cuadrados, puedes usar el botón `x²`.
- **Raíces:**
  - **Raíz cuadrada:** Usa el botón `√`. Ejemplo: `√`, luego `81`, luego `=` para obtener `9`.
  - **Raíz cúbica:** Busca el botón `³√`.
  - **Raíz n-ésima:** Busca el botón `ⁿ√` o `ˣ√`. Ejemplo: para `⁴√16`, teclea `4`, luego `ⁿ√`, luego `16` y presiona `=` para obtener `2`.