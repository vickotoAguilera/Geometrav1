
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Solo los usuarios autenticados pueden leer y escribir en su propia carpeta de subidas.
    match /uploads/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
